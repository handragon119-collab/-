# --- 핵심 ---
anthropic>=0.40.0          # Claude로 캡션/해시태그 생성
python-dotenv>=1.0.0       # .env 환경변수 로드
Pillow>=10.0.0             # 로컬 이미지 폴백/후처리
requests>=2.31.0

# --- 이미지 생성 (택1, 선택사항) ---
openai>=1.40.0             # DALL·E 3 / gpt-image-1 사용 시
google-generativeai>=0.8.0 # Gemini 이미지 생성 사용 시

# --- 인스타그램 업로드 (택1) ---
instagrapi>=2.1.0          # 일반 계정 비공식 API 업로드 시
# Graph API 방식은 requests만 있으면 됨 (추가 의존성 없음)
